# 第五阶段学习指南：实践与实验

> **适合人群**：已完成前四个阶段学习  
> **预计时间**：持续（根据实验需求）  
> **目标**：通过实践和实验深入理解系统，优化性能，扩展功能

---

## 📚 学习目标

完成第五阶段后，你应该能够：

1. ✅ 运行和测试整个系统
2. ✅ 进行性能分析和优化
3. ✅ 添加新功能和组件
4. ✅ 进行A/B测试和实验
5. ✅ 理解生产环境的部署

---

## 🎯 第一部分：环境搭建和运行

### 1.1 开发环境要求

**Rust 环境**：
- Rust 1.70+（推荐使用 rustup 安装）
- Cargo（Rust 包管理器）

**Python 环境**：
- Python 3.10+
- [uv](https://docs.astral.sh/uv/getting-started/installation/)（Python 包管理器）

**其他工具**：
- Git
- 代码编辑器（推荐 VS Code 或 RustRover）

### 1.2 安装步骤

#### 1. 安装 Rust

```bash
# 安装 rustup
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 验证安装
rustc --version
cargo --version
```

#### 2. 安装 Python 和 uv

```bash
# 安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# 验证安装
uv --version
```

#### 3. 克隆项目

```bash
git clone <repository-url>
cd x-algorithm
```

### 1.3 运行代码

#### 运行 Phoenix 模型测试

```bash
# 进入 phoenix 目录
cd phoenix

# 运行检索模型
uv run run_retrieval.py

# 运行排序模型
uv run run_ranker.py

# 运行测试
uv run pytest test_recsys_model.py test_recsys_retrieval_model.py
```

#### 编译 Rust 代码

```bash
# 编译整个项目
cargo build

# 运行测试
cargo test

# 运行特定测试
cargo test --test <test_name>
```

### 1.4 任务清单

- [ ] 搭建开发环境
- [ ] 成功运行 Phoenix 模型测试
- [ ] 成功编译 Rust 代码
- [ ] 理解项目结构

---

## 🔬 第二部分：实验和修改

### 2.1 实验1：修改过滤器顺序

**目标**：理解过滤器顺序的重要性

**步骤**：

1. **阅读当前过滤器顺序**：
   ```rust
   // home-mixer/candidate_pipeline/phoenix_candidate_pipeline.rs
   let filters: Vec<Box<dyn Filter<...>>> = vec![
       Box::new(DropDuplicatesFilter),
       Box::new(CoreDataHydrationFilter),
       Box::new(AgeFilter::new(...)),
       // ...
   ];
   ```

2. **修改顺序**：
   - 将 `AgeFilter` 移到最前面
   - 观察结果变化

3. **分析影响**：
   - 记录候选数量变化
   - 理解为什么顺序重要

**预期结果**：
- 过滤器顺序会影响最终结果
- 某些过滤器必须在其他过滤器之后

### 2.2 实验2：调整打分权重

**目标**：理解权重对排序的影响

**步骤**：

1. **找到权重配置**：
   ```rust
   // home-mixer/params.rs 或类似文件
   pub const FAVORITE_WEIGHT: f64 = 1.0;
   pub const REPLY_WEIGHT: f64 = 0.8;
   pub const RETWEET_WEIGHT: f64 = 0.9;
   // ...
   ```

2. **修改权重**：
   - 增加 `FAVORITE_WEIGHT` 到 2.0
   - 观察排序结果变化

3. **分析影响**：
   - 记录排序变化
   - 理解权重如何影响最终分数

**预期结果**：
- 权重越高，对应动作的影响越大
- 需要平衡不同动作的权重

### 2.3 实验3：添加新过滤器

**目标**：实现一个新功能

**需求**：实现一个 `LanguageFilter`，只保留特定语言的帖子

**步骤**：

1. **创建新文件**：
   ```bash
   touch home-mixer/filters/language_filter.rs
   ```

2. **实现过滤器**：
   ```rust
   use crate::candidate_pipeline::candidate::PostCandidate;
   use crate::candidate_pipeline::query::ScoredPostsQuery;
   use tonic::async_trait;
   use xai_candidate_pipeline::filter::{Filter, FilterResult};

   pub struct LanguageFilter {
       pub allowed_languages: Vec<String>,
   }

   impl LanguageFilter {
       pub fn new(allowed_languages: Vec<String>) -> Self {
           Self { allowed_languages }
       }
   }

   #[async_trait]
   impl Filter<ScoredPostsQuery, PostCandidate> for LanguageFilter {
       async fn filter(
           &self,
           query: &ScoredPostsQuery,
           candidates: Vec<PostCandidate>,
       ) -> Result<FilterResult<PostCandidate>, String> {
           let (kept, removed): (Vec<_>, Vec<_>) = candidates
               .into_iter()
               .partition(|c| {
                   // 检查帖子的语言是否在允许列表中
                   // 需要从 candidate 中获取语言信息
                   // 这里假设有 language_code 字段
                   self.allowed_languages.contains(&c.language_code)
               });
           
           Ok(FilterResult { kept, removed })
       }
   }
   ```

3. **集成到管道**：
   ```rust
   // home-mixer/candidate_pipeline/phoenix_candidate_pipeline.rs
   let filters: Vec<Box<dyn Filter<...>>> = vec![
       // ... 其他过滤器
       Box::new(LanguageFilter::new(vec!["zh".to_string(), "en".to_string()])),
   ];
   ```

4. **测试**：
   - 运行代码
   - 验证过滤器工作正常

**预期结果**：
- 成功实现新过滤器
- 理解如何添加新组件

### 2.4 实验4：性能分析

**目标**：识别性能瓶颈

**步骤**：

1. **添加性能日志**：
   ```rust
   use std::time::Instant;

   async fn execute(&self, query: Q) -> PipelineResult<Q, C> {
       let start = Instant::now();
       
       let hydrated_query = self.hydrate_query(query).await;
       let query_time = start.elapsed();
       info!("Query hydration took: {:?}", query_time);
       
       let start = Instant::now();
       let candidates = self.fetch_candidates(&hydrated_query).await;
       let source_time = start.elapsed();
       info!("Candidate sourcing took: {:?}", source_time);
       
       // ... 其他阶段
       
       let total_time = start.elapsed();
       info!("Total pipeline time: {:?}", total_time);
   }
   ```

2. **分析结果**：
   - 找出最耗时的阶段
   - 分析原因
   - 提出优化建议

3. **优化**：
   - 实现优化方案
   - 验证性能提升

**预期结果**：
- 识别性能瓶颈
- 理解各阶段的耗时分布

### 2.5 任务清单

- [ ] 完成实验1：修改过滤器顺序
- [ ] 完成实验2：调整打分权重
- [ ] 完成实验3：添加新过滤器
- [ ] 完成实验4：性能分析
- [ ] 记录实验结果和发现

---

## 🚀 第三部分：性能优化

### 3.1 并行优化

**目标**：最大化并行执行

**检查点**：

1. **Query Hydrators**：
   - 确保所有 hydrators 可以并行执行
   - 检查是否有依赖关系

2. **Sources**：
   - 确保所有 sources 可以并行执行
   - 检查网络请求是否阻塞

3. **Hydrators**：
   - 确保所有 hydrators 可以并行执行
   - 检查批量请求是否优化

### 3.2 缓存优化

**目标**：减少重复计算

**缓存策略**：

1. **用户特征缓存**：
   - 缓存用户关注列表
   - 缓存用户偏好设置

2. **ML 预测缓存**：
   - 利用 Candidate Isolation，缓存候选分数
   - 缓存用户嵌入

3. **数据缓存**：
   - 缓存帖子核心数据
   - 缓存作者信息

### 3.3 批量优化

**目标**：减少网络请求次数

**优化点**：

1. **批量获取数据**：
   - 合并多个请求
   - 使用批量 API

2. **批量 ML 预测**：
   - 一次预测多个候选
   - 减少模型调用次数

### 3.4 任务清单

- [ ] 分析并行执行情况
- [ ] 实现缓存机制
- [ ] 优化批量请求
- [ ] 测量性能提升

---

## 🧪 第四部分：A/B 测试

### 4.1 设计 A/B 测试

**目标**：比较不同配置的效果

**示例测试**：

1. **测试不同权重配置**：
   - A组：默认权重
   - B组：增加点赞权重
   - 指标：用户点赞率、停留时间

2. **测试不同过滤器顺序**：
   - A组：当前顺序
   - B组：调整后的顺序
   - 指标：候选数量、用户满意度

### 4.2 实现 A/B 测试框架

**步骤**：

1. **添加实验配置**：
   ```rust
   pub struct ExperimentConfig {
       pub weight_config: WeightConfig,
       pub filter_order: FilterOrder,
       // ...
   }
   ```

2. **根据用户分组**：
   ```rust
   fn get_experiment_group(user_id: u64) -> ExperimentGroup {
       // 使用用户ID的哈希值进行分组
       let hash = hash_user_id(user_id);
       if hash % 2 == 0 {
           ExperimentGroup::A
       } else {
           ExperimentGroup::B
       }
   }
   ```

3. **记录指标**：
   ```rust
   fn record_metrics(
       user_id: u64,
       group: ExperimentGroup,
       metrics: Metrics,
   ) {
       // 记录指标到数据库或日志
   }
   ```

### 4.3 分析结果

**步骤**：

1. **收集数据**：
   - 用户交互数据
   - 性能指标

2. **统计分析**：
   - 计算平均值
   - 进行显著性检验

3. **得出结论**：
   - 哪个配置更好
   - 是否应该上线

### 4.4 任务清单

- [ ] 设计一个 A/B 测试
- [ ] 实现 A/B 测试框架
- [ ] 运行测试
- [ ] 分析结果

---

## 📊 第五部分：监控和调试

### 5.1 添加监控指标

**关键指标**：

1. **性能指标**：
   - 请求延迟（P50, P95, P99）
   - 各阶段耗时
   - 吞吐量（QPS）

2. **质量指标**：
   - 候选数量
   - 过滤率
   - 用户交互率

3. **错误指标**：
   - 错误率
   - 超时率
   - 失败组件

### 5.2 实现监控

**步骤**：

1. **添加指标收集**：
   ```rust
   use prometheus::{Counter, Histogram};

   lazy_static! {
       static ref REQUEST_COUNT: Counter = Counter::new(
           "requests_total",
           "Total number of requests"
       ).unwrap();
       
       static ref REQUEST_DURATION: Histogram = Histogram::with_opts(
           HistogramOpts::new("request_duration_seconds", "Request duration")
       ).unwrap();
   }
   ```

2. **暴露指标端点**：
   ```rust
   // 在 HTTP 服务器中添加 /metrics 端点
   ```

3. **可视化**：
   - 使用 Grafana 等工具
   - 创建仪表板

### 5.3 调试技巧

**技巧**：

1. **使用日志**：
   - 添加详细的日志
   - 使用结构化日志（JSON格式）

2. **使用追踪**：
   - 使用 `request_id` 追踪请求
   - 使用分布式追踪系统（如 Jaeger）

3. **使用调试器**：
   - 设置断点
   - 检查变量值

### 5.4 任务清单

- [ ] 添加监控指标
- [ ] 实现监控系统
- [ ] 创建仪表板
- [ ] 实践调试技巧

---

## 🎓 第六部分：扩展功能

### 6.1 添加新数据源

**目标**：添加一个新的候选源

**步骤**：

1. **实现 Source trait**：
   ```rust
   pub struct NewSource {
       // ...
   }

   #[async_trait]
   impl Source<ScoredPostsQuery, PostCandidate> for NewSource {
       async fn get_candidates(&self, query: &ScoredPostsQuery) -> Result<Vec<PostCandidate>, String> {
           // 实现获取候选的逻辑
       }
   }
   ```

2. **集成到管道**：
   ```rust
   let sources: Vec<Box<dyn Source<...>>> = vec![
       Box::new(ThunderSource { ... }),
       Box::new(PhoenixSource { ... }),
       Box::new(NewSource { ... }),  // 新增
   ];
   ```

### 6.2 添加新打分器

**目标**：实现一个新的打分逻辑

**步骤**：

1. **实现 Scorer trait**：
   ```rust
   pub struct NewScorer {
       // ...
   }

   #[async_trait]
   impl Scorer<ScoredPostsQuery, PostCandidate> for NewScorer {
       async fn score(&self, query: &ScoredPostsQuery, candidates: &[PostCandidate]) -> Result<Vec<PostCandidate>, String> {
           // 实现打分逻辑
       }
   }
   ```

2. **集成到管道**：
   ```rust
   let scorers: Vec<Box<dyn Scorer<...>>> = vec![
       Box::new(PhoenixScorer { ... }),
       Box::new(WeightedScorer),
       Box::new(NewScorer { ... }),  // 新增
   ];
   ```

### 6.3 任务清单

- [ ] 添加一个新数据源
- [ ] 添加一个新打分器
- [ ] 测试新功能
- [ ] 文档化新功能

---

## ✅ 第七步：自我检查

### 检查清单

完成以下检查，确保你掌握了：

#### 实践能力
- [ ] 我能运行和测试代码吗？
- [ ] 我能修改和优化代码吗？
- [ ] 我能添加新功能吗？

#### 性能优化
- [ ] 我能识别性能瓶颈吗？
- [ ] 我能实现优化方案吗？
- [ ] 我能测量性能提升吗？

#### 实验能力
- [ ] 我能设计 A/B 测试吗？
- [ ] 我能分析实验结果吗？
- [ ] 我能得出结论吗？

---

## 📝 学习笔记模板

```
# 第五阶段学习笔记

## 日期：____

## 环境搭建
[记录环境搭建过程]

## 实验记录
实验1：修改过滤器顺序
[结果和发现]

实验2：调整打分权重
[结果和发现]

实验3：添加新过滤器
[实现过程和结果]

实验4：性能分析
[性能瓶颈和优化]

## 性能优化
[优化方案和效果]

## A/B 测试
[测试设计和结果]

## 扩展功能
[新功能实现]

## 收获
[记录学到的知识]
```

---

## 🚀 下一步

完成第五阶段后，你应该：

1. ✅ 能够独立进行实验和优化
2. ✅ 能够添加新功能
3. ✅ 能够进行性能优化

**恭喜！你已经完成了所有阶段的学习！**

现在你可以：
- 深入研究某个特定领域
- 贡献代码到项目
- 在实际项目中应用所学知识

---

## 🎉 学习总结

### 学习路径回顾

1. **第一阶段**：理解整体架构
2. **第二阶段**：理解数据流
3. **第三阶段**：深入各组件
4. **第四阶段**：理解 ML 模型
5. **第五阶段**：实践与实验

### 关键收获

- ✅ 理解了推荐系统的完整架构
- ✅ 理解了各个组件的工作原理
- ✅ 理解了 ML 模型的设计
- ✅ 掌握了实践和优化技能

### 继续学习

- 阅读相关论文
- 参与开源项目
- 在实际项目中应用
- 分享学习经验

---

**祝你学习顺利，实践成功！🎉**

记住：实践是检验真理的唯一标准，多实验，多思考，多总结！
